package com.chemiconsult.repository;

import com.chemiconsult.entity.ResolucionDE;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResolucionRepository extends JpaRepository<ResolucionDE, Long> {
}
